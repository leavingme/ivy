# Use hoisted node_modules for compatibility with packages that expect
# npm's flat structure (e.g. some Next.js plugins, Tailwind).
node-linker=hoisted
# Auto-install peers (pnpm is strict by default; this avoids peer warnings)
auto-install-peers=true
